# @cryprice/shared

**Public stub — shared types and constants**

This package holds minimal shared symbols for the monorepo (`packages/api-client` depends on it). It is a **placeholder**, not a complete shared library.

Expand types and constants here as the public SDK matures. See [`docs/public-api.md`](../../docs/public-api.md).

```bash
npm install
npm run typecheck
```
