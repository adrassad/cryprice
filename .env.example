# Корневые переменные (общие для локальной разработки монорепо).
# Скопируйте в `.env` и заполните значения.

# URL публичного API (для apps и examples)
CRYPRICE_PUBLIC_API_URL=http://localhost:3001

# Опционально: ключ для демо/лимитов (если понадобится)
# CRYPRICE_PUBLIC_API_KEY=
