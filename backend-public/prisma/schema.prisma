// Prisma schema — расширьте моделями по мере разработки backend-public

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// model Example {
//   id String @id @default(cuid())
// }
